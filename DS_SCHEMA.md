# DS Schema Reference

Auto-generated by `extract_ds_schema.py` on 2026-04-25. Source of truth for code that touches DS structs, constants, or helper signatures. **Re-run after any header edit.**

All patcher scripts (`fix_combat_step1.py`, future BASIC ROM batch patcher, etc.) should reference this doc rather than rely on stock-ROM/ROT memory. The `Discovered deltas` section at the bottom is hand-curated and survives re-runs of this script.

## Constant blocks

Every `#define PREFIX_xxx VALUE` we found, grouped by prefix. The `Source` column points to the file:line so you can verify or find related context.

### `ROOM_*`

_33 entries._

| Symbol | Value | Source |
|---|---|---|
| `ROOM_VNUM_ALOSER` | `25419` | merc.h:1216 |
| `ROOM_VNUM_ALTAR` | `8071` | merc.h:1125 |
| `ROOM_VNUM_APOCALYPSE` | `20021` | merc.h:1154 |
| `ROOM_VNUM_ARTIFICER` | `8114` | merc.h:1141 |
| `ROOM_VNUM_AWINNER` | `25420` | merc.h:1215 |
| `ROOM_VNUM_BALANCE` | `4500` | merc.h:1128 |
| `ROOM_VNUM_CASINO_CARDS` | `8195` | merc.h:1138 |
| `ROOM_VNUM_CASINO_DICE` | `8196` | merc.h:1139 |
| `ROOM_VNUM_CASINO_SLOTS` | `8067` | merc.h:1137 |
| `ROOM_VNUM_CATHEDRAL1` | `559` | merc.h:1144 |
| `ROOM_VNUM_CATHEDRAL2` | `560` | merc.h:1145 |
| `ROOM_VNUM_CATHEDRAL3` | `561` | merc.h:1146 |
| `ROOM_VNUM_CATHEDRAL4` | `562` | merc.h:1147 |
| `ROOM_VNUM_CHAIN` | `7914` | merc.h:1131 |
| `ROOM_VNUM_CHAT` | `1200` | merc.h:1121 |
| `ROOM_VNUM_CLANS` | `20000` | merc.h:1132 |
| `ROOM_VNUM_CORNER` | `3` | merc.h:1119 |
| `ROOM_VNUM_DEMISE` | `4201` | merc.h:1129 |
| `ROOM_VNUM_HONOR` | `4300` | merc.h:1130 |
| `ROOM_VNUM_LEGION` | `20090` | merc.h:1151 |
| `ROOM_VNUM_LIMBO` | `2` | merc.h:1118 |
| `ROOM_VNUM_MORGUE` | `4` | merc.h:1120 |
| `ROOM_VNUM_PARADOX` | `8197` | merc.h:1217 |
| `ROOM_VNUM_PERDITION` | `20000` | merc.h:1153 |
| `ROOM_VNUM_PIT` | `8000` | merc.h:1124 |
| `ROOM_VNUM_PREDATOR` | `11599` | merc.h:1156 |
| `ROOM_VNUM_PROPHECY` | `20072` | merc.h:1152 |
| `ROOM_VNUM_REGISTERPK` | `8079` | merc.h:1142 |
| `ROOM_VNUM_SCHOOL` | `8197` | merc.h:1127 |
| `ROOM_VNUM_SERPENTIS` | `20063` | merc.h:1155 |
| `ROOM_VNUM_TEMPLE` | `8084` | merc.h:1122 |
| `ROOM_VNUM_WAR_CHAMPION` | `2652` | merc.h:1135 |
| `ROOM_VNUM_WAR_ENTRANCE` | `6468` | merc.h:1134 |

### `CON_*`

_9 entries._

| Symbol | Value | Source |
|---|---|---|
| `CON_BREAK_CONNECT` | `6` | merc.h:435 |
| `CON_CONFIRM_NEW_NAME` | `3` | merc.h:432 |
| `CON_COPYOVER_RECOVER` | `7` | merc.h:436 |
| `CON_CREATION` | `8` | merc.h:437 |
| `CON_GET_NAME` | `1` | merc.h:430 |
| `CON_GET_OLD_PASSWORD` | `2` | merc.h:431 |
| `CON_PLAYING` | `0` | merc.h:429 |
| `CON_READ_IMOTD` | `4` | merc.h:433 |
| `CON_READ_MOTD` | `5` | merc.h:434 |

### `TYPE_*`

_3 entries._

| Symbol | Value | Source |
|---|---|---|
| `TYPE_HIT` | `1000` | merc.h:1871 |
| `TYPE_MOCK` | `-2` | merc.h:1869 |
| `TYPE_UNDEFINED` | `-1` | merc.h:1870 |

### `DIR_*`

_6 entries._

| Symbol | Value | Source |
|---|---|---|
| `DIR_DOWN` | `5` | merc.h:1175 |
| `DIR_EAST` | `1` | merc.h:1171 |
| `DIR_NORTH` | `0` | merc.h:1170 |
| `DIR_SOUTH` | `2` | merc.h:1172 |
| `DIR_UP` | `4` | merc.h:1174 |
| `DIR_WEST` | `3` | merc.h:1173 |

### `MAX_*`

_43 entries._

| Symbol | Value | Source |
|---|---|---|
| `MAX_ALIAS` | `30` | merc.h:213 |
| `MAX_ALLY` | `4` | merc.h:256 |
| `MAX_BEACON` | `3` | merc.h:216 |
| `MAX_BUF` | `65536` | recycle.h:37 |
| `MAX_BUF_LIST` | `12` | recycle.h:38 |
| `MAX_CCOLOR` | `6` | merc.h:135 |
| `MAX_CLAN` | `8` | merc.h:219 |
| `MAX_DAMAGE_MESSAGE` | `242` | merc.h:210 |
| `MAX_DIR` | `12` | olc.h:99 |
| `MAX_DUPES` | `7` | merc.h:217 |
| `MAX_FORGET` | `30` | merc.h:214 |
| `MAX_FORMS` | `5` | merc.h:212 |
| `MAX_GLOBAL` | `1` | merc.h:265 |
| `MAX_GLOBAL` | `1` | music.h:37 |
| `MAX_GROUP` | `140` | merc.h:206 |
| `MAX_INPUT_LENGTH` | `256` | merc.h:186 |
| `MAX_IN_GROUP` | `31` | merc.h:207 |
| `MAX_KEY_HASH` | `1024` | merc.h:184 |
| `MAX_KILLSTATS` | `4` | merc.h:233 |
| `MAX_LEADER` | `4` | merc.h:202 |
| `MAX_LEVEL` | `110` | merc.h:222 |
| `MAX_LINES` | `100` | merc.h:264 |
| `MAX_LINES` | `100` | music.h:36 |
| `MAX_LIQUID` | `36` | merc.h:209 |
| `MAX_MEM_LIST` | `12` | merc.h:188 |
| `MAX_MOB` | `5` | olc.h:6 |
| `MAX_PC_RACE` | `32` | merc.h:218 |
| `MAX_POLY` | `6` | merc.h:910 |
| `MAX_QUEST` | `27` | quest.h:62 |
| `MAX_RANK` | `8` | merc.h:201 |
| `MAX_SKILL` | `700` | merc.h:205 |
| `MAX_SOCIALS` | `1000` | merc.h:204 |
| `MAX_SONGS` | `100` | merc.h:263 |
| `MAX_SONGS` | `50` | music.h:35 |
| `MAX_STATS` | `7` | merc.h:577 |
| `MAX_STRING_LENGTH` | `4608` | merc.h:185 |
| `MAX_TACTIC` | `4` | merc.h:1278 |
| `MAX_TRACK` | `20` | merc.h:208 |
| `MAX_TRADE` | `5` | merc.h:561 |
| `MAX_TYPOS` | `7` | merc.h:211 |
| `MAX_VIEW` | `30` | merc.h:215 |
| `MAX_WAR` | `10` | merc.h:257 |
| `MAX_WARTYPE` | `4` | merc.h:1205 |

### `LEVEL_*`

_4 entries._

| Symbol | Value | Source |
|---|---|---|
| `LEVEL_HERO` | `(MAX_LEVEL - 9)` | merc.h:224 |
| `LEVEL_IMMORTAL` | `(MAX_LEVEL - 8)` | merc.h:225 |
| `LEVEL_IMPLEMENTOR` | `(MAX_LEVEL)` | merc.h:226 |
| `LEVEL_NEWBIE` | `20` | merc.h:223 |

### `PULSE_*`

_10 entries._

| Symbol | Value | Source |
|---|---|---|
| `PULSE_AREA` | `(120 * PULSE_PER_SECOND)` | merc.h:240 |
| `PULSE_AUCTION` | `( 10 * PULSE_PER_SECOND)` | merc.h:242 |
| `PULSE_MOBILE` | `(  4 * PULSE_PER_SECOND)` | merc.h:237 |
| `PULSE_MUSIC` | `(  4 * PULSE_PER_SECOND)` | merc.h:238 |
| `PULSE_PER_SECOND` | `4` | merc.h:235 |
| `PULSE_QUEST` | `( 30 * PULSE_PER_SECOND)` | merc.h:241 |
| `PULSE_SLOWTRAP` | `(  6 * PULSE_PER_SECOND)` | merc.h:244 |
| `PULSE_TICK` | `( 60 * PULSE_PER_SECOND)` | merc.h:239 |
| `PULSE_VIOLENCE` | `(  3 * PULSE_PER_SECOND)` | merc.h:236 |
| `PULSE_WAR` | `( 30 * PULSE_PER_SECOND)` | merc.h:243 |

## Struct definitions

Verbatim from the source headers. When writing C that touches these, copy field names from here — DON'T trust your memory of stock ROM.

### `struct char_data`

From `merc.h`:

```c
struct  char_data
{
    CHAR_DATA *         next;
    CHAR_DATA *         next_in_room;
    CHAR_DATA *         polymorphed;
    CHAR_DATA *		stalking;
    CHAR_DATA *         master;
    CHAR_DATA *         leader;
    CHAR_DATA *         fighting;
    CHAR_DATA *         reply;
    CHAR_DATA *         pet;
    MEM_DATA *          memory;
    SPEC_FUN *          spec_fun;
    MOB_INDEX_DATA *    pIndexData;
    DESCRIPTOR_DATA *   desc;
    AFFECT_DATA *       affected;
    AFFECT_DATA *	effected; // Quick affects.
    NOTE_DATA *         pnote;
    OBJ_DATA *          carrying;
    OBJ_DATA *          was_carrying;
    OBJ_DATA *          on;
    ROOM_INDEX_DATA *   in_room;
    ROOM_INDEX_DATA *   was_in_room;
    ROOM_INDEX_DATA *	paradox_to_room;
    AREA_DATA *         zone;
    PC_DATA *           pcdata;
    CLAN_DATA *         clan;
    CLAN_DATA *         invited;
    GEN_DATA *          gen_data;
    GRANT_DATA *	granted;
    GRANT_DATA *	revoked;
    int			polyform;
    bool                valid;
    bool                hastimer;
    bool                attacker;
    bool                on_quest;
    bool                wedpost;
    bool		ordered;
    char *              name;
    long                id;
    bool		trivia_playing;
    int			trivia_score;
    char *		trivia_response;
    sh_int              version;
    char *              short_descr;
    char *              long_descr;
    char *              die_descr;
    char *              say_descr;
    char *              description;
    char *              prompt;
    char *              prefix;
    sh_int              group;
    sh_int              clead;
    sh_int              sex;
    sh_int              pclass;
    sh_int              race;
    sh_int		rank;
    sh_int              level;
    sh_int              trust;
    int                 played;
    int                 lines;  /* for the pager */
    int                 stunned;
    int                 snared;
    time_t              llogoff;
    time_t              logon;
    sh_int              timer;
    sh_int              wait;
    sh_int              flee_daze;
    sh_int              skill_daze;
    sh_int              spell_daze;
    long                hit;
    long                max_hit;
    sh_int              mana;
    sh_int              max_mana;
    sh_int              move;
    sh_int              max_move;
    long                platinum;
    long                gold;
    long                silver;
    long                exp;
    int                 iquest;
    long                act;
    long                comm;   /* RT added to pad the vector */
    long                chan;
    long                wiznet; /* wiz stuff */

    long                imm_flags;
    long                res_flags;
    long                vuln_flags;
    long		absorb_flags;

    long		qdone;
    long		gclan;
    sh_int              invis_level;
    sh_int              incog_level;
    sh_int              ghost_level;
    long		revokebits;
    long		grantbits;
    long                battle;
    long		status;
    long                affected_by;
    long		affected_by2;
    long                shielded_by;
    long		defense_flags;
    int                 dam_bonus[25];
    sh_int              position;
    sh_int              practice;
    sh_int              train;
    sh_int              carry_weight;
    sh_int              carry_number;
    sh_int              saving_throw;
    sh_int              alignment;
    sh_int              hitroll;
    sh_int              damroll;
    sh_int              armor[4];
    sh_int		magic_resist;
    sh_int		magic_reflect;
    sh_int		toughness;
    sh_int		regain;
    sh_int		speed;
    sh_int              wimpy;
    /* tracking */
    sh_int              track_to[MAX_TRACK];
    sh_int              track_from[MAX_TRACK];
    /* stats */
    sh_int              perm_stat[MAX_STATS];
    sh_int              mod_stat[MAX_STATS];
    /* parts stuff */
    long                form;
    long                parts;
    sh_int              size;
    sh_int              material;
    /* mobile stuff */
    long                off_flags;
    sh_int              damage[3];
    sh_int		dam_flags;
    sh_int              dam_type;
    sh_int              start_pos;
    sh_int              default_pos;

};
```

### `struct pc_data`

From `merc.h`:

```c
struct  pc_data
{
    PC_DATA *           next;
    CHAR_DATA *         chat;
    BUFFER *            buffer;
    bool                valid;
    char *              pwd;
    char *		true_name;
    char *		true_descr;

    char *              socket;
    time_t              last_note;
    time_t              last_idea;
    time_t              last_penalty;
    time_t              last_questinfo;
    time_t              last_changes;
    time_t              last_claninfo;
    time_t		last_classifieds;
    time_t		last_immnote;
    sh_int              perm_hit;
    sh_int              perm_mana;
    sh_int              perm_move;
    sh_int              true_sex;
    int                 last_level;
    int                 recall;
    int                 hrecall;
    int                 tier;
    int			security;
    int                 bounty;
    CHAR_DATA *         questgiver;
    sh_int              quest_attem;
    sh_int              quest_fail;
    sh_int              nextquest;
    sh_int              countdown;
    sh_int              questobj;
    sh_int              questmob;
    sh_int              quest_accum;
    sh_int              quest_curr;
    sh_int              tells;
    int                 components[MAX_COMPONENT];
    int                 clan_pair;
    int                 heads;
    int                 class_saves;
    int                 melee_fail;
    int                 pkkills;
    int                 pkdeaths;
    int                 pxflee;
    int                 pxkills;
    int                 pkpoints;
    long		totalkill_pc;
    long		totalkill_npc;
    long		totaldeath_pc;
    long		totaldeath_npc;
    long gamestat[MAX_KILLSTATS];



	//** Arena Record.
    int                 awins;      /* ARENA number of wins */
    int                 alosses;    /* ARENA number of losses */
    int			atimer;	    /* ARENA timer */
    int                 wager[5][2];
    char 		*arena_record[10];
    int  		arena_wins;
    int   		arena_losses;
    int   		arena_pts;

	//** War Record.
	sh_int				war_kills	[MAX_WARTYPE];
	sh_int				war_losses	[MAX_WARTYPE];
	sh_int				war_wins	[MAX_WARTYPE];

    sh_int		registerpk;
    sh_int              clan_rank;
    sh_int              followers;
    sh_int              condition [4];
    sh_int              learned [MAX_SKILL];
    bool                group_known [MAX_GROUP];
    sh_int              points;
    bool                confirm_delete;
    bool                confirm_reroll;
    char *              forget[MAX_FORGET];
    char *              view[MAX_VIEW];
    char *              dupes[MAX_DUPES];
    char *              alias[MAX_ALIAS];
    char *              alias_sub[MAX_ALIAS];
    long                b_platinum;
    long                b_gold;
    int                 killtime;
    int                 pktimer;
    int                 stimer;
    int                 dance;
    int			aura;
    int			wind;
    int                 stance; //*** Fighting stances for monks.
    long                permres;
    long		permvuln;
    sh_int              color;
    sh_int		ccolor[MAX_CCOLOR];

	//*** Customizable strings.
	char *				walkin;
	char *				walkout;
	char *				pretitle;
	char *				sayself;
	char *				sayothers;
	char *				poseself;
	char *				poseothers;
	char *				castself;
	char *				castothers;
	char *				entergame;
	char *				quitgame;
	char *				disappear;
	char *				reappear;
     	char *              		bamfin;
    	char *              		bamfout;
    	char *              		title;
    	char *              		who_descr;
	char *				fatality;

	sh_int				recall_vnum;
	sh_int				stage[3];
	CHAR_DATA *			partner;
	bool				virgin;
	int				dopple_vnum;
	int				forms[MAX_FORMS];
    	sh_int              		true_stat[MAX_STATS];
	sh_int				true_race;
};
```

### `struct exit_data`

From `merc.h`:

```c
struct exit_data
{
    	EXIT_DATA *		next;
	ROOM_INDEX_DATA *	in_room;
	ROOM_INDEX_DATA *	to_room;

    sh_int	to_vnum;
    sh_int	exit_flags;
    sh_int	key;
    sh_int	lock_dif;
    char *	keyword;
    char *	description;
    char *	door_descr;
    int		rs_flags;
    int		orig_door;
};
```

### `struct room_index_data`

From `merc.h`:

```c
struct  room_index_data
{
    ROOM_INDEX_DATA *   next;
    CHAR_DATA *         people;
    OBJ_DATA *          contents;
    EXTRA_DESCR_DATA *  extra_descr;
    AREA_DATA *         area;
    EXIT_DATA *         exit    [12];
    RESET_DATA *	reset_first;
    RESET_DATA *	reset_last;

    char *              name;
    char *              description;
    char *              owner;
    sh_int              vnum;
    int                 room_flags;
    sh_int              light;
    sh_int              sector_type;
    sh_int              heal_rate;
    sh_int              mana_rate;
    sh_int              clan;
};
```

### `struct obj_data`

From `merc.h`:

```c
struct  obj_data
{
    OBJ_DATA *          next;
    OBJ_DATA *          next_content;
    OBJ_DATA *          contains;
    OBJ_DATA *          in_obj;
    OBJ_DATA *          on;
    CHAR_DATA *         carried_by;
    EXTRA_DESCR_DATA *  extra_descr;
    AFFECT_DATA *       affected;
    OBJ_INDEX_DATA *    pIndexData;
    ROOM_INDEX_DATA *   in_room;
    OSPEC_FUN *		spec_fun;

    long		id;
    bool                valid;
    bool                enchanted;
    bool		vnum;
    bool		damaged;
    char *              owner;
    char *              killer;
    char *              name;
    char *              short_descr;
    char *		content_descr;
    char *              description;
    sh_int              item_type;
    int			size_flags;		// Sizes that can use item.
    int                 extra_flags;
    int                 wear_flags;
    sh_int              wear_loc;
    sh_int              weight;
    int                 cost;
    sh_int              level;
    sh_int              condition;
    sh_int              material;
    sh_int              timer;
    int                 value   [5];
    sh_int              clan;
    sh_int              class_bits;
};
```

### `struct obj_index_data`

From `merc.h`:

```c
struct  obj_index_data
{
    OBJ_INDEX_DATA *    next;
    EXTRA_DESCR_DATA *  extra_descr;
    AFFECT_DATA *       affected;
    AREA_DATA *		area;
    OSPEC_FUN *		spec_fun;
    sh_int		max_world;
    char *              name;
    char *              short_descr;
    char *		content_descr;
    char *              description;
    sh_int              vnum;
    sh_int              reset_num;
    sh_int              material;
    sh_int              item_type;
    int			size_flags;		// Sizes that can use item.
    int                 extra_flags;
    int                 wear_flags;
    sh_int              level;
    sh_int              condition;
    sh_int              count;
    sh_int              weight;
    int                 cost;
    int                 value[5];
    sh_int              clan;
    sh_int              class_bits;
};
```

### `struct descriptor_data`

From `merc.h`:

```c
struct  descriptor_data
{
    DESCRIPTOR_DATA *   next;
    DESCRIPTOR_DATA *   snoop_by;
#if defined(unix)
    pthread_t 		dnsThread;
#endif
    CHAR_DATA *         character;
    CHAR_DATA *         original;
    bool                valid;
    bool		ansi_color;
    char                host[HOST_LEN+1];
    unsigned int        descriptor;
    sh_int              connected;
    sh_int              creation;
    bool                fcommand;
    char                inbuf           [4 * MAX_INPUT_LENGTH];
    char                incomm          [MAX_INPUT_LENGTH];
    char                inlast          [MAX_INPUT_LENGTH];
    int                 repeat;
    char *              outbuf;
    int                 outsize;
    int                 outtop;
    char *              showstr_head;
    char *              showstr_point;
    char *              run_buf;
    char *              run_head;
    bool		append;
    void *              pEdit;
    void *              pSubEdit;
    char **             pString;
    int			editor;
};
```

### `struct affect_data`

From `merc.h`:

```c
struct  affect_data
{
    AFFECT_DATA *       next;
    bool                valid;
    sh_int		from;
    long		id;
    sh_int              where;
    sh_int              type;
    sh_int              level;
    sh_int              duration;
    sh_int              location;
    sh_int              modifier;
    int                 bitvector;
};
```

### `struct mob_index_data`

From `merc.h`:

```c
struct  mob_index_data
{
    MOB_INDEX_DATA *    next;
    SPEC_FUN *          spec_fun;
    SHOP_DATA *         pShop;
    AREA_DATA *		area;
    sh_int		max_world;
    sh_int              vnum;
    sh_int              group;
    sh_int              count;
    sh_int              killed;

    char *              keyword_list;
    char *              short_descr;
    char *              long_descr;
    char *              die_descr;
    char *              say_descr;
    char *              description;

    long                act;
    long                battle;
    long                affected_by;
    long		affected_by2;
    long                shielded_by;
    sh_int              alignment;
    sh_int              level;
    sh_int              hit[3];
    sh_int              mana[3];
    sh_int              ac[4];
    sh_int              hitroll;
    sh_int		damroll;
    sh_int		saving_throw;
    sh_int		magic_resist;
    sh_int		magic_reflect;
    sh_int		toughness;
    sh_int		regain;
    sh_int		speed;
    sh_int              damage[3];
    int			dam_flags;
    sh_int              dam_type;

    long                off_flags;
    long                imm_flags;
    long                res_flags;
    long                vuln_flags;
    long		defense_flags;
    long		absorb_flags;

    sh_int              start_pos;
    sh_int              default_pos;
    sh_int              sex;
    sh_int              race;
    long                wealth;
    long                form;
    long                parts;
    sh_int              size;
    sh_int              material;
};
```

### `struct area_data`

From `merc.h`:

```c
struct  area_data
{
    	AREA_DATA *         		next;
    	RESET_DATA *        		reset_first;
    	RESET_DATA *        		reset_last;
	HELP_AREA *			helps;
    	char *              		file_name;
    	char *              		name;
    	char *              		credits;
    	sh_int              		age;
    	sh_int              		nplayer;
    	sh_int              		low_range;
    	sh_int              		high_range;
    	sh_int              		min_vnum;
   	sh_int              		max_vnum;
    	bool                		empty;
	char *				description;
    	char *				builders;
    	int				vnum;
    	int				area_flags;
    	int				security;
	sh_int				low_level;
	sh_int				high_level;
};
```

## Skill-number globals (`gsn_*`)

Affect / skill numbers used by `is_affected(ch, gsn_xxx)` and spell dispatch. Only declared `gsn_*` symbols are usable — if you want to gate a flee on `gsn_petrify` and it's not in this list, you'll get an undeclared-identifier error.

_None found._

## Helper function signatures

Prototypes for the helpers we call most often. Whitespace has been collapsed for readability. If a helper appears here with more arguments than you remember from stock ROM, **use the DS version** — that's why we run this script.

| Helper | Prototype (from headers) |
|---|---|
| `multi_hit` | `void multi_hit args( ( CHAR_DATA *ch, CHAR_DATA *victim, int dt ) );` |
| `one_hit` | _not found in headers_ |
| `damage` | `int damage args( ( CHAR_DATA *ch, CHAR_DATA *victim, int dam, int dt, int damtype, int show ) );` |
| `is_safe` | `bool is_safe args( ( CHAR_DATA *ch, CHAR_DATA *victim, bool fShow ) );` |
| `is_safe_spell` | `bool is_safe_spell args( ( CHAR_DATA *ch, CHAR_DATA *victim, bool area ) );` |
| `set_fighting` | _not found in headers_ |
| `stop_fighting` | `void stop_fighting args( ( CHAR_DATA *ch, bool fBoth ) );` |
| `update_pos` | `void update_pos args( ( CHAR_DATA *victim ) );` |
| `check_killtimer` | `void check_killtimer args( ( CHAR_DATA *ch ) );` |
| `move_char` | `void move_char args( ( CHAR_DATA *ch, int door, bool follow, bool quiet ) );` |
| `find_door` | _not found in headers_ |
| `affect_to_char` | `void affect_to_char args( ( CHAR_DATA *ch, AFFECT_DATA *paf ) );` |
| `affect_remove` | `void affect_remove args( ( CHAR_DATA *ch, AFFECT_DATA *paf ) );` |
| `affect_strip` | `void affect_strip args( ( CHAR_DATA *ch, int sn ) );` |
| `affect_join` | `void affect_join args( ( CHAR_DATA *ch, AFFECT_DATA *paf ) );` |
| `is_affected` | `bool is_affected args( ( CHAR_DATA *ch, int sn ) );` |
| `affect_join` | `void affect_join args( ( CHAR_DATA *ch, AFFECT_DATA *paf ) );` |
| `affect_modify` | _not found in headers_ |
| `get_char_room` | `CD * get_char_room args( ( CHAR_DATA *ch, char *argument ) );` |
| `get_char_world` | `CD * get_char_world args( ( CHAR_DATA *ch, char *argument ) );` |
| `get_obj_room` | _not found in headers_ |
| `get_obj_world` | `OD * get_obj_world args( ( CHAR_DATA *ch, char *argument ) );` |
| `get_obj_carry` | `OD * get_obj_carry args( ( CHAR_DATA *ch, char *argument ) );` |
| `get_obj_wear` | `OD * get_obj_wear args( ( CHAR_DATA *ch, char *argument ) );` |
| `get_room_index` | `RID * get_room_index args( ( int vnum ) );` |
| `send_to_char` | `void send_to_char args( ( const char *txt, CHAR_DATA *ch ) );` |
| `act` | `void act args( ( const char *format, CHAR_DATA *ch, const void *arg1, const void *arg2, int type ) );` |
| `page_to_char` | `void page_to_char args( ( const char *txt, CHAR_DATA *ch ) );` |
| `write_to_buffer` | `void write_to_buffer args( ( DESCRIPTOR_DATA *d, const char *txt, int length ) );` |
| `number_door` | `int number_door args( ( void ) );` |
| `number_percent` | `int number_percent args( ( void ) );` |
| `number_range` | `int number_range args( ( int from, int to ) );` |
| `number_bits` | `int number_bits args( ( int width ) );` |
| `number_fuzzy` | `int number_fuzzy args( ( int number ) );` |
| `number_mm` | `long number_mm args( ( void ) );` |
| `dice` | `int dice args( ( int number, int size ) );` |
| `one_argument` | `char * one_argument args( ( char *argument, char *arg_first ) );` |
| `first_arg` | _not found in headers_ |
| `is_name` | `bool is_name args( ( char *str, char *namelist ) );` |
| `fread_string` | `char * fread_string args( ( FILE *fp ) );` |
| `str_dup` | `char * str_dup args( ( const char *str ) );` |
| `get_trust` | `int get_trust args( ( CHAR_DATA *ch ) );` |
| `gain_exp` | `void gain_exp args( ( CHAR_DATA *ch, int gain ) );` |
| `save_char_obj` | `void save_char_obj args( ( CHAR_DATA *ch ) );` |
| `load_char_obj` | `bool load_char_obj args( ( DESCRIPTOR_DATA *d, char *name ) );` |
| `skill_lookup` | `int skill_lookup args( ( const char *name ) );` |
| `find_spell` | `int find_spell args( ( CHAR_DATA *ch, const char *name) );` |
| `check_dispel` | _not found in headers_ |
| `do_function` | `void do_function args( ( CHAR_DATA *ch, DO_FUN *fun, char *argument ) );` |
| `mud_logf` | `void mud_logf (char * fmt, ...) __attribute__ ((format(printf,1,2)));` |
| `log_string` | `void log_string args( ( const char *str ) );` |
| `wiznet` | `void wiznet args( (char *string, CHAR_DATA *ch, OBJ_DATA *obj, long flag, long flag_skip, int min_level ) );` |

## Discovered deltas from stock ROM

<!-- DELTAS-BEGIN: hand-curated; preserved across re-runs -->

Lessons we've learned the hard way. Update this list any time a compile
failure reveals a new mismatch between stock ROM/ROT idiom and DS reality.

| What stock ROM has | What DS has | Discovered |
|---|---|---|
| `pexit->u1.to_room` | `pexit->to_room` | 2026-04-25, do_flee port |
| `pexit->exit_info` | `pexit->exit_flags` | 2026-04-25, do_flee port |
| `move_char(ch, door, follow)` 3-arg | `move_char(ch, door, follow, quiet)` 4-arg | 2026-04-25, do_flee port |
| `ch->daze` (single counter) | `ch->flee_daze` / `ch->skill_daze` / `ch->spell_daze` (split) | 2026-04-25, do_flee port |
| `is_safe(ch, victim)` 2-arg | `is_safe(ch, victim, show_msg)` 3-arg | 2026-04-25, do_kill port |
| `POS_STUNNED` available as #define | Not in any scanned header — use literal `5` (verified by disasm `cmpl $0x5`). The schema extractor's regex only matches `#define`, not enums; POS_* probably lives in an enum somewhere. Extractor v2 should fix this. | 2026-04-25, do_kill port |
| Stock ROM's `move_char` doesn't auto-look on arrival | DS's `move_char(quiet=FALSE)` already prints destination room. Don't follow it with an explicit `do_look` — that doubles the output. | 2026-04-25, do_flee port |
| Stock ROM's `learned[]` lookup | DS uses a `get_skill(ch, sn)` helper that URANGE-clamps based on class min level, race, and current level before returning learned[sn]. Always go through `get_skill` rather than reading `pcdata->learned[sn]` directly — otherwise level/class gates don't apply. | 2026-04-25, do_kick port (Code) |
| Stock ROM has one set of affect flags (`affected_by`) | DS has TWO: `affected_by` (long, original AFF_*) and `affected_by2` (long, extended AFF2_*). New affects added by DS like `AFF2_LIMPING` live in the second word. Confirmed `affected_by2` field at offset in struct char_data; AFF2_* bit values to be captured by extractor v2. | 2026-04-25, do_kick port (Code) |
| Class indexes (partial) | Class index `38` = knight (Julian's class). Class index `40` = elder. Full class table to be dumped as part of `SKILL_TABLE.md` post-`extract_skill_table.py` run. | 2026-04-25, do_kick port (Code) |
| Race indexes (partial) | Race index `1` = fay. Races `{8, 26, 44}` form the "multi_kick path" set in do_kick — bigger / kick-specialised races take a different damage path. | 2026-04-25, do_kick port (Code) |
| `gsn_knee_breaker` exists | DS-specific skill number for the knee-breaker side-mechanic on do_kick. Class-restricted to elder at level 80; everyone else gated behind IMM (102). When extractor v2 catches enums, gsn_* declarations should also be re-checked since v1 missed all of them. | 2026-04-25, do_kick port (Code) |
| `set_fighting` declared in headers | Not declared in any header the schema extractor scanned — defined in `fight.c` and used by act_combat verbs without prototype. Code's batch port called it without issue, so DS's `fight.c` exposes it via global linkage; just add a local extern in any new act_combat function that calls it, or rely on implicit declaration. | 2026-04-25, do_rescue port (Code) |
| Stock disarm() helper signature | The stub generator's auto-stub for the lowercase `disarm()` helper had the wrong signature. DS's real signature (verified end-to-end against disasm at `0x003c..0x0273`) takes `(CHAR_DATA *ch, CHAR_DATA *victim)` and handles WEAR_WIELD removal, !REMOVE/!DROP/!INVENTORY/QUEST flag handling, the room-drop, the NPC-grab-back chance, and the secondary→primary slot cascade across DS's 4 wield slots (WEAR_WIELD / WEAR_SECONDARY / WEAR_THIRD / WEAR_FOURTH). | 2026-04-25, disarm helper port (Code) |
| `multi_kick` signature | Confirmed against disasm. (Code captured exact signature in act_combat.c during the do_kick port; refer to that file for the canonical prototype until extractor catches it.) | 2026-04-25, do_kick port (Code) |
| Position enum values | Confirmed in DS: `POS_DEAD`, `POS_MORTAL`, `POS_INCAP`, `POS_STUNNED=5`, `POS_SLEEPING`, `POS_RESTING`, `POS_SITTING`, `POS_FIGHTING=6`, `POS_STANDING`. The extractor v1 missed the enum block; v2 needs an enum parser. Until then, use the literal value with a comment, as we did in do_kill. | 2026-04-25, multiple ports (Code) |
| `gsn_*` extern declaration pattern | DS's `gsn_*` skill numbers aren't declared with `extern sh_int gsn_xxx;` (what the v1 extractor looked for). Code identified the actual declaration form during the do_berserk port. Extractor v2 needs to widen its regex to match DS's pattern; until then, treat any `gsn_*` reference as needing manual verification before use. | 2026-04-25, do_berserk port (Code) |
| AFFECT_DATA struct reuse idiom | When applying multiple affect modifiers from one ability (e.g., do_berserk applies HITROLL + DAMROLL + AC adjustments), DS code builds a single `AFFECT_DATA af` struct, calls `affect_to_char(ch, &af)`, then mutates `af.location` / `af.modifier` and calls again. The `next` pointer doesn't need resetting between calls because `affect_to_char` copies the struct. Memorise this idiom — it'll appear in every multi-modifier ability port. | 2026-04-25, do_berserk port (Code) |
| `skill_table[gsn].beats` and `min_mana` columns are authoritative for ability cost | They're not. The disasm wins — many DS abilities ignore beats / min_mana from skill_table and hardcode their own wait/cost in the function body. Treat skill_table columns as informational reference only; always pull the actual values from the disasm. | 2026-04-25, do_spellbane port (Code) |
| Already-buffed check uses `IS_AFFECTED(ch, AFF_X)` (bitvector) | DS canonical check is `is_affected(ch, gsn_X)` against the gsn — not the AFF_ bit. Don't double up with both unless the disasm shows both checks. The bitvector check matters only when the affect carries an AFF_ bit (some don't — see next row). | 2026-04-25, do_spellbane port (Code) |
| AFFECT_DATA always has a non-zero bitvector | Wrong. `bitvector = 0` is a legitimate stat-only-modifier pattern — the affect modifies a stat (HITROLL, SAVING_SPELL, etc.) without setting any AFF_ flag. Don't invent a bit for an affect just because stock ROM does — match the disasm. | 2026-04-25, do_spellbane port (Code) |
| `TAR_CHAR_SELF` skills run the standard arg-handling pass | They don't. Self-targeted abilities skip `one_argument` / `get_char_room` entirely — the prologue jumps straight to the get_skill check. Spot-check via the disasm prologue before adding arg-parsing code. | 2026-04-25, do_spellbane port (Code) |
| All skill-check sites use `get_skill(ch, sn)` | Most do, but `do_warcry` reads `pcdata->learned[gsn_X]` directly (raw access, no URANGE clamp). Per-port from disasm — don't unify on `get_skill` without checking. | 2026-04-25, BUFF-SELF batch (Code) |
| `check_improve` always fires on success with mult=1 | Polarity and multipliers vary widely. `do_warcry` fires `check_improve` only on the FAIL path with `success_flag=TRUE` (likely "improve on near-misses" design or 2003 bug). `do_concentration` and `do_ironwill` use mult=4. Per-port from disasm. | 2026-04-25, BUFF-SELF batch (Code) |
| Mana/move costs deducted after the roll on success path | Some abilities deduct BEFORE the roll regardless of outcome (`do_concentration`, `do_ironwill` subtract upfront). Others deduct different amounts on success vs fail (`do_resistance`, `do_spellbane`). Per-port from disasm. | 2026-04-25, BUFF-SELF batch (Code) |
| `extract_obj(OBJ_DATA *)` signature | Single-arg, removes the object from the world. Used by abilities that consume an item on use (e.g., `do_warpaint` consumes a skull whether the paint succeeds or fails — the "skull is wasted" failure path also extracts it). | 2026-04-25, do_warpaint port (Code) |
| `OBJ_VNUM_SKULL = 56` | DS-specific item vnum referenced by `do_warpaint` and likely other tribal/voodoo flavour abilities. Worth noting because the abilities care about a specific vnum, not a flag or item_type. | 2026-04-25, do_warpaint port (Code) |
| Already-buffed message uses `send_to_char` | Mixed — some abilities use `act(...TO_CHAR)` for already-buffed messages (visible to room-watchers as flavour), others use `send_to_char` (private). Per-port from disasm. | 2026-04-25, BUFF-SELF batch (Code) |
| `AFFECT_DATA.bitvector` always sets an AFF_ bit | The bitvector field is per-`where`: AFF_* for `where=TO_AFFECTS`, AFF2_* for `where=TO_AFFECTS2`, RES_* for `where=TO_RESIST`, IMM_* for `where=TO_IMMUNE`. `do_ironwill` exploits this — single affect sets RES_CHARM via `where=TO_RESIST` AND modifies APPLY_SAVES via location/modifier. The where field controls which constant family the bitvector uses. | 2026-04-25, do_ironwill port (Code) |
| Wait clamp values come from `skill_table[gsn].beats` | Sometimes — but functions can also use a hardcoded literal (e.g., `do_target` clamps to 6 directly, ignoring whatever `skill_table[gsn_target].beats` says). Pull the literal from disasm; treat the skill_table value as informational only. | 2026-04-25, do_target port (Code) |
| Argument parsing always goes through `one_argument` | Some verbs skip it and pass `argument` directly to `get_char_room` (e.g., `do_target` does `argument[0] == '\0'` raw check, then `get_char_room(ch, argument)` — no whitespace stripping). Pattern, not requirement; per-port from disasm. | 2026-04-25, do_target port (Code) |
| Wait clamp always runs before `check_improve` | Order varies. BUFF-SELF cluster clamps wait early; `do_target` clamps wait at the bottom of the function, AFTER `check_improve`. Functionally equivalent (both happen before return), but worth noting for diff-cleanliness when comparing per-function ports. | 2026-04-25, do_target port (Code) |
| STRIKE-PURE chance scaling — pull literal per-port from disasm | Each STRIKE-PURE function applies a scaling factor to the base skill chance before the percent roll. `do_cleave` uses `chance * 4 / 5` (80%). Other cluster members will likely differ — do not assume 4/5 universally. Always read the multiplication/division literal from the disasm (look for `imul`/`idiv` near the `number_percent()` call). | 2026-04-26, do_cleave disasm (Code) |
| HIT damage helper choice varies across STRIKE-PURE | Three options seen across the cluster: `one_hit(ch, victim, gsn, 0)` (uses weapon stats — cleave uses this), `damage(ch, victim, dam, gsn, DAM_type, TRUE)` (explicit damage amount), `multi_hit(ch, victim, gsn)` (multi-strike). Which each uses is per-port from disasm. Check the call just after the hit-path `check_improve` — the function name tells you which path. `one_hit` 4-arg form confirmed in DS (4th arg = secondary weapon flag, 0 = primary). | 2026-04-26, do_cleave disasm (Code) |
| STRIKE-PURE victim-alert flags vary across cluster | Disasm 0x66b1 in `do_cleave`: `[victim+0x17e] & 0x40` zeros chance (victim is "alert" / in a defensive state). Three different bytes seen across STRIKE-PURE / related cluster functions at offsets 0x17c..0x17f, with different bit masks. Struct field at 0x17e not yet named — schema extractor v2 needed to map CHAR_DATA offsets in that region. Using the raw offset cast `((unsigned char*)victim)[0x17e] & 0x40` as a proxy until resolved. Gate skipped in current do_cleave port. | 2026-04-26, do_cleave disasm (Code) |

| AOE STRIKE-PURE PvP attacker mark | All four AOE STRIKE-PURE functions share a pattern: `if (!ch->fighting && both are PCs) { ch->attacker = TRUE; vch->attacker = FALSE; }` — marks the initiating PC as attacker on first-strike out of combat. Reusable pattern for the rest of the cluster. | 2026-04-26, AOE batch (Code) |
| AOE STRIKE-PURE stomp double is_safe | `do_stomp` calls `is_safe(ch, vch, FALSE)` twice — once in the room loop gate (standard skip), and again inside the safe-target branch to print "You miss." instead of silently skipping. Preserves the timestop edge case: a target in timestop shows "You miss." rather than invisible skip. Disasm-faithful; do not collapse to one call. | 2026-04-26, AOE batch (Code) |
| AOE STRIKE-PURE whirlwind variable swings | `do_whirlwind` calls `one_hit` 1–3 times per target via a fresh per-target percent roll: >90% → 3 swings, >50% → 2 swings, else 1 swing. This roll is independent of the initial chance roll. The only variable-swing-count STRIKE-PURE function found so far. | 2026-04-26, AOE batch (Code) |
| CHAR_DATA offsets confirmed (2026-04-26) | `offsetof` against merc.h confirmed: `fighting=0x18`, `in_room=0x4c`, `attacker=0x7a`, `level=0xcc`, `stunned=0xdc`, `wait=0xf0`, `act=0x12c`, `move=0x110`, `position=0x1e4`. All match disasm reads exactly. | 2026-04-26, AOE batch offset verification (Code) |
| Position values previously listed as `POS_FIGHTING=6, POS_STUNNED=5` | Wrong — superseded. Verified against `bit.h:911-917`. The `BIT(name, str, value, settable)` macro (bit.h:21) expands to `enum {name = value};`, so the bit.h table is authoritative. Correct values: `POS_DEAD=0, POS_MORTAL=1, POS_SLEEPING=2, POS_RESTING=3, POS_SITTING=4, POS_FIGHTING=5, POS_STANDING=6`. There is NO `POS_STUNNED` or `POS_INCAP` in bit.h — the earlier delta inferred those from a different source. `do_stomp`'s gate `cmp [esi+0x1e4], 0x4; jg ...` reads as "position must be > POS_SITTING (4)". | 2026-04-26, AOE batch (Code) |
| AOE STRIKE-PURE non-engaged filter | NOT cluster-universal. Only `do_legsweep` filters out non-engaged targets via `(ch->fighting == vch \|\| vch->fighting == ch)`. `do_dhammer`, `do_stomp`, `do_whirlwind` target every non-self/non-safe/non-group character in the room regardless of combat state. The "AOE-restricted-to-engaged" pattern is a legsweep quirk, not a cluster property — don't replicate without disasm confirmation. | 2026-04-26, AOE batch (Code) |
| AOE STRIKE-PURE chance scaling | NOT applied for the four AOE functions confirmed so far (legsweep, dhammer, stomp, whirlwind). Raw `get_skill` value goes straight into `number_percent() < chance`. The `chance * 4 / 5` literal seen in `do_cleave` is single-target STRIKE-PURE only — don't carry it over to AOE ports. | 2026-04-26, AOE batch (Code) |
| AOE WAIT_STATE pattern | Cluster-universal across STRIKE-PURE AOE: `if (get_trust(ch) > 101) ch->wait = 0; else if (skill_table[gsn].beats > ch->wait) ch->wait = skill_table[gsn].beats;`. IMM bypass + max-clamp. The `> 101` constant is the threshold for trust-level-102 (IMM) bypass. The `skill_table[gsn].beats` access materialises as `[gsn*0x240 + 0x228]` in disasm (sizeof skill_table entry = 0x240, beats field at offset 0x228). | 2026-04-26, AOE batch (Code) |
| `fire_effect` signature | `void fire_effect(void *vo, int level, int dam, int target);` — `vo` is the target as a void*, `target` is one of `TARGET_CHAR=0`/`TARGET_OBJ=1`/`TARGET_ROOM=2`/`TARGET_NONE=3` (merc.h:1886). `do_dhammer` calls `fire_effect(vch, ch->level, 100, TARGET_CHAR)` AFTER `one_hit`, with a hardcoded `dam=100` (NOT scaled by the weapon damage from `one_hit`). Pattern for "weapon swing + elemental followup". | 2026-04-26, do_dhammer port (Code) |
| ROOM_SAFE bit position in room_index_data | Disasm reads `test BYTE PTR [eax+0x5d], 0x4` where `eax = ch->in_room`. So `room_index_data->room_flags` (long) lives at offset 0x5c, and the high-byte bit 0x4 (within byte at 0x5d) corresponds to `ROOM_SAFE`. The macro `IS_SAFE(ch)` (`merc.h:2017`) wraps this as `IN_SAFE(ch) && !paradox_is_on()`. All four AOE functions use this gate — IS_SAFE check makes whole-room safe-zones honored, with paradox suppression as the override. | 2026-04-26, AOE batch (Code) |
| `damage()` 6-arg call shape (verified from disasm) | `int damage(CHAR_DATA *ch, CHAR_DATA *victim, int dam, int dt, int damtype, int show);` — `dt` is gsn (or `TYPE_HIT`/`TYPE_UNDEFINED`), `damtype` is one of the `DAM_*` enum values from `bit.h:191+` (DAM_NONE=0, DAM_BASH=1, DAM_PIERCE=2, DAM_SLASH=3, DAM_FIRE=4, ...), `show` is bool. Stomp uses `damage(ch, vch, dam, gsn_stomp, DAM_BASH, TRUE)`. | 2026-04-26, do_stomp port (Code) |
| `gsn.h` already extern-declares all `gsn_*` symbols | The `GSN(_x)` macro in `gsn.h:7-11` expands to `extern sh_int _x;` when `GSN_SOURCE` is undefined (i.e. everywhere except `db.c`). `merc.h:305` includes `gsn.h`, so any file that includes `merc.h` already has every `gsn_*` declared. The inline `extern sh_int gsn_xxx;` declarations in `do_target` and `do_cleave` are redundant — `do_legsweep` and the AOE batch ports use the bare symbol. Don't add inline externs in new ports. | 2026-04-26, AOE batch (Code) |
| `act()` `type` arg values | Confirmed against `merc.h:540-544`: `TO_ROOM=0, TO_NOTVICT=1, TO_VICT=2, TO_CHAR=3, TO_ALL=4`. Not the stock-ROM ordering some references use. AOE caster broadcast on HIT typically does `TO_ROOM` (the "$n …" line) THEN `TO_CHAR` (the "You …" line). MISS broadcast same order. Per-target damage messages dispatched by `damage()` / `one_hit()` use TO_VICT / TO_NOTVICT internally. | 2026-04-26, AOE batch (Code) |
| STRIKE-PURE single-target HP-fraction divisor | NOT cluster-universal. `do_cleave` skips the gate entirely; `do_cross_slash` uses `/10` (fail when victim is below 10% HP — `imul 0x66666667 + sar 2` reciprocal idiom); `do_garotte` and `do_backstab` use `/3` (fail below 33% — `imul 0x55555556` reciprocal). Pull the divisor from disasm — never assume one-third. The reciprocal magic-number tells you the divisor: `0x55555556 ≈ 2^32 / 3`, `0x66666667 ≈ 2^32 / 10 × (something)` (verify by computing `2^32 / divisor` in Python and matching). | 2026-04-26, cross_slash + garotte port (Code) |
| `affected_by2` bit 15 chance halver (garotte) | `do_garotte` reads `cmpw [victim+0x174], 0; jns ...` — that's `affected_by2` low 16 bits as `sh_int`, "negative" iff bit 15 set. When set, `chance /= 2`. Flag name unknown; field at CHAR_DATA offset `0x174` confirmed by disasm. Schema extractor v2 needs to map AFF2_* bit names. Until then, encode as raw offset access. | 2026-04-26, do_garotte port (Code) |
| `is_gangbanger(ch, victim)` — wait-clamp doubler | New helper pattern. `do_garotte`'s post-roll wait calc multiplies `skill_table[gsn].beats` by 2 if `is_gangbanger` returns TRUE (gang-up scenario penalty). Disasm signature: `shl eax, 1` after the skill_table beats read. Trust > 101 still bypasses to wait=0 either way (IMM override applies before the doubler). May appear in other PvP-flavoured ports. | 2026-04-26, do_garotte port (Code) |
| Sleeping-victim auto-HIT idiom (garotte / backstab) | The roll formula isn't pure `number_percent < chance`. Garotte uses `(number_percent < chance) || (chance > 1 && victim->position <= POS_SLEEPING)` — auto-HIT against a sleeping victim provided base chance is non-trivial. Backstab carries a similar pattern in our existing port. Confirm whether do_strike / do_gore reuse this when those land. | 2026-04-26, do_garotte port (Code) |
| Pre-helper caster broadcast presence — NOT cluster-universal | `do_cleave` and `do_cross_slash` emit 3 `act()` broadcasts (TO_VICT, TO_CHAR, TO_NOTVICT) before calling the HIT helper (`one_hit` / `multi_hit`). `do_garotte` skips the pre-helper broadcasts entirely — `multi_hit`'s internal combat messages do all the work. Pull broadcast strings from disasm `.rodata`; don't invent them. | 2026-04-26, do_garotte port (Code) |
| `one_argument` + `get_char_room` is canonical explicit-target idiom | When a STRIKE-PURE single-target ability takes an explicit target (rather than `ch->fighting`), the prologue is: `char arg[MAX_INPUT_LENGTH]; one_argument(argument, arg);` then `if (arg[0] == '\0') ...fail message; victim = get_char_room(ch, arg);`. Seen in `do_target`, `do_garotte`. Stack buffer is `MAX_INPUT_LENGTH`. Whitespace-stripping via `one_argument` is consistent in this idiom; raw `argument` access (as in some `do_target` paths) is the alternative — pick from disasm. | 2026-04-26, do_garotte port (Code) |
| Gate order is per-port within STRIKE-PURE single-target | Not a sub-cluster property — each function arranges its early-exit gates differently. `do_cross_slash`: get_skill → dual-wield → fighting → rift → HP-fraction → stunned → can_see → wait. `do_cleave`: get_skill → fighting → wield → rift → stunned → can_see → wait. The weapon vs. fighting check ordering specifically can flip per port. New ports must pull gate order from disasm before assuming the prior port's order. | 2026-04-26, cross_slash port (Code) |
| Dual-wield gate — both WEAR_WIELD AND WEAR_SECONDARY | `do_cross_slash` requires BOTH slots occupied. Either empty fails before the fighting check with appropriate message. Different from `do_cleave`'s single-weapon gate. New "dual-wield required" ports should use the same idiom: separate `get_eq_char(ch, WEAR_WIELD)` and `get_eq_char(ch, WEAR_SECONDARY)` checks, each with its own fail message. | 2026-04-26, do_cross_slash port (Code) |
| `WEAPON_WHIP = 7` (verify before assuming weapon-type checks gate ITEM_WEAPON) | `do_garotte`'s weapon check is `weapon->value[0] == WEAPON_WHIP` ONLY — does NOT also test `item_type == ITEM_WEAPON`. A non-weapon obj with `value[0]=7` would technically pass (practically impossible, but disasm-faithful means we don't paper over it). Pattern: weapon-type-specific gates check `value[0]` against the WEAPON_* constant, not item_type. | 2026-04-26, do_garotte port (Code) |

| STRIKE-PURE single-target chance scaling — third value | NEW: `do_gore` uses `× 9/10` (disasm: `lea ebx,[eax+eax*8]; imul 0x66666667; sar 2`, i.e. `chance × 9 / 10`). The full STRIKE-PURE single-target table is now: cleave `× 4/5`, cross_slash NO scaling, garotte NO scaling, gore `× 9/10`, strike NO scaling. Per-port from disasm — no cluster default. | 2026-04-26, do_gore port (Code) |
| STRIKE-PURE victim-alert flag at byte 0x17d, bit 0x2 (do_strike) | `do_strike` reads `[victim+0x17d] & 0x2`. If set, chance is **zeroed out** (not halved like garotte's AFF2 bit-15). The MISS-path broadcast also branches on this flag — alert vs not-alert produces two distinct 3-line broadcast sets. Adds to the existing per-port victim-alert delta (cross_slash uses 0x17e & 0x40, garotte uses AFF2 bit 15). Schema extractor v2 needs to map all CHAR_DATA bytes in the 0x17c..0x180 region and the AFF2_* bit names. | 2026-04-26, do_strike port (Code) |
| STRIKE-PURE single-target HIT helper — direct damage option | NEW: `do_gore` uses `damage(ch, victim, dam, gsn, DAM_PIERCE, TRUE)` directly on HIT — NOT `multi_hit` or `one_hit`. Damage formula is `number_range(ch->level * 2, ch->level * 4) + 100` (level-scaled, with a +100 floor). Only STRIKE-PURE single-target ability without a melee-helper call. Used because gore is a single dramatic horn-thrust, not a melee round. | 2026-04-26, do_gore port (Code) |
| Secondary-roll bonus on HIT — victim-stun (do_gore) | NEW: after the primary HIT path resolves, `do_gore` rolls `number_percent() < chance / 4`. If true, sets `victim->stunned = number_range(1, 2)` and fires a 3-line broadcast announcing the stun. First STRIKE-PURE with a HIT-conditional secondary effect. The bonus chance is gated by the primary chance, so high-skill casters get the stun more often. | 2026-04-26, do_gore port (Code) |
| Secondary-roll penalty on MISS — caster self-stun (do_gore) | NEW: after a MISS, `do_gore` rolls `number_percent() > chance / 2`. If true, sets `ch->stunned = number_range(1, 2)` and fires a 2-line broadcast (TO_ROOM + TO_CHAR) describing the caster falling over. **First STRIKE-PURE with a caster self-effect.** Note the inequality: `>` chance/2, meaning the WORSE half of the miss roll triggers self-stun. High-skill casters miss less AND when they miss, they're more likely to be in the better half of the roll, so self-stun rate scales inversely with skill. | 2026-04-26, do_gore port (Code) |
| do_gore gate-order — no HP-fraction, no weapon check | `do_gore` is the only STRIKE-PURE single-target without an HP-fraction gate (cross_slash has /10, garotte has /3, strike has /10). Also no weapon check. Gates: get_skill → target resolution → can_see → ch->stunned → self-target → is_safe → check_killtimer. The race lock is **implicit** through `skill_table[gsn_gore]`'s class/race level table — non-horned classes/races have skill at 0%, hitting the get_skill <= 0 gate with the "Grow some horns first." message. | 2026-04-26, do_gore port (Code) |
| do_strike vs do_cross_slash — single-weapon gate, MISS-path two variants | `do_strike` shares cross_slash's HP-frac /10, multi_hit HIT helper, and gangbanger wait-doubler, but: (1) checks WEAR_WIELD only (single weapon), not WEAR_WIELD + WEAR_SECONDARY (dual-wield). (2) MISS path branches on the alert flag with two complete 3-line broadcast sets ("$N avoids" vs "$N dodges"). cross_slash's MISS is single-variant. (3) Adds caster rift gate `is_affected(ch, gsn_rift)` — caster cannot strike while THEY are rifted (cross_slash checks something different). | 2026-04-26, do_strike port (Code) |

| Class + Dance precondition (do_storm) | For class indices 16, 34, 52 (dance-using monk variants), `do_storm` requires `pcdata->dance == 2` (Storm Dance state) unless IMM. Other classes skip the gate. Disasm: `cmp [ch+0xc0], 0x10/0x22/0x34` then `cmp [pcdata+0xf6c], 0x2`. The pcdata offset 0xf6c maps to `pc_data.dance` per merc.h:1603. NEW pattern: dance/stance state as a class-conditional gate. | 2026-04-27, do_storm port (Code) |
| STRIKE-PURE AOE with mana cost | First STRIKE-PURE AOE that costs mana — `do_storm` requires `ch->mana > 249` and spends 150 on success. Other AOE in the cluster (legsweep, dhammer, stomp, whirlwind) are free or move-cost. Distinguishes "weapon-skill AOE" from "magical-style AOE" within the cluster. Mana spend happens AFTER the gate but BEFORE the percent roll, so failed casts still drain mana. | 2026-04-27, do_storm port (Code) |
| "Huh?" as the standard `get_skill <= 0` reply | Most ports use a verb-specific failure message ("You strike a pose.", "Grow some horns first.", "What are you smoking?"). do_storm and do_cleanse both use the generic catch-all `str1.1:0x5e` "Huh?". Worth noting as an alternative idiom — pull the actual string from disasm for each new port; don't assume a custom message exists. | 2026-04-27, do_storm + do_cleanse ports (Code) |
| STRIKE-PURE victim-alert flags — cluster table extended | Every port confirmed has a DIFFERENT alert byte/bit combo. Updated table: cleave (none), cross_slash byte 0x17e bit 0x40 (chance=0), garotte AFF2 bit 15 via `cmpw [+0x174]` (chance/=2), strike byte 0x17d bit 0x2 (chance=0), charge byte 0x17c bit 0x40 (chance=0). Cluster-wide rule: never reuse another port's offset+bit; always pull fresh from disasm. The bytes 0x17c..0x17f are a 4-byte region on CHAR_DATA that probably maps to some "alert" / "guard" / "stance" status; schema extractor v2 needs to name it. | 2026-04-27, do_charge port (Code) |
| `effect_to_char` quick-affect with hardcoded level | DS uses `effect_to_char(victim, &af)` (the "quick affects" / `effected` chain — distinct from `affect_to_char` which goes on `affected`). do_charge applies gsn_wary to victim with af.level = 101 hardcoded (NOT ch->level), af.duration = 10 ticks, af.location = APPLY_NONE, af.modifier = 0, af.bitvector = 0. Pattern: marker-only quick-affect with hardcoded level prevents normal level-based dispel. Likely re-used by other "cooldown" mechanics (do_pinch, do_eadbutt, do_blackjack). | 2026-04-27, do_charge port (Code) |

When a new delta is discovered: add a row, commit DS_SCHEMA.md, and reference
the row from the patcher's comments so future-Claude doesn't repeat the mistake.

<!-- DELTAS-END -->
